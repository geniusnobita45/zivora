# Optional production Postgres connection. Without this, development leads are stored in .data/leads.json.
DATABASE_URL=

# Change before deploying. Used to sign the admin session cookie.
ADMIN_PASSWORD=change-this-password
SESSION_SECRET=replace-with-a-long-random-secret
